Ось лаконічний та професійний `README.md`, який описує твій проект. Він допоможе тобі структурувати файли та швидко згадати, як усе запустити, якщо повернешся до коду через місяць.

---

# 🛰️ CyberQuest: Team Edition

**CyberQuest** — це інтерактивна платформа для проведення кібербезпекових змагань у класі. Проект реалізований як синхронізована гра: учні об’єднуються у команди («столи»), чекають сигналу вчителя та змагаються в реальному часі.

## 🚀 Основні можливості

* **Синхронний старт**: Гравці перебувають у «листі очікування», поки адміністратор не запустить гру.
* **Командний залік**: 6 столів по 6 гравців. У командний бал зараховується лише **найкращий особистий результат** кожного гравця.
* **Інтерактивні завдання**: Текстові питання та аналіз зображень (Deepfake detection).
* **Реальний час**: Адмін-панель з візуалізацією заповнення столів та прогресу.
* **Matrix Style**: Футуристичний дизайн у стилі кіберпанк.

## 🛠 Технологічний стек

* **Frontend**: HTML5, CSS3 (Flexbox/Grid), JavaScript (Vanilla).
* **Backend**: Python 3.10 (Google Cloud Functions).
* **Database**: Google Firestore (NoSQL).
* **Infrastructure**: Terraform (IaC) та Google Cloud Storage.

## 📂 Структура проекту

```text
/
├── backend/
│   ├── main.py            # Логіка API та взаємодія з Firestore
│   ├── requirements.txt   # Залежності (google-cloud-firestore)
│   └── teams_config.json  # Список назв команд
├── frontend/
│   ├── index.html         # Основна сторінка гравця
│   ├── admin.html         # Панель керування вчителя
│   ├── script.js          # Логіка клієнта, таймер, API-запити
│   ├── style.css          # Стилі та анімації
│   ├── questions.js       # База питань (JSON-like)
│   └── img/               # Зображення для завдань "Знайди фейк"
└── main.tf                # Конфігурація інфраструктури Terraform

```

## ⚙️ Розгортання (Deployment)

1. **Налаштування GCP**: Переконайся, що у тебе встановлений [Google Cloud SDK](https://cloud.google.com/sdk) та активований проект.
2. **Ініціалізація Terraform**:
```bash
terraform init

```


3. **Створення інфраструктури**:
```bash
terraform apply -auto-approve

```


4. **Зв'язок Frontend з API**:
* Після виконання команди скопіюй отриманий `api_endpoint`.
* Встав його в змінну `API_URL` у файлах `frontend/script.js` та `frontend/admin.html`.
* Виконай `terraform apply` ще раз, щоб оновити файли в хмарному сховищі.



## 🔐 Адміністрування

* **Пароль адміна**: Встановлюється в `main.tf` через змінну середовища `ADMIN_PASSWORD`.
* **Скидання гри**: Кнопка **Reset** в адмінці повністю очищує статистику та повертає всіх гравців у початкове меню.

---

*Розроблено для освітніх цілей у межах презентації з кібербезпеки 2026.*

---

**Що ще додати?** Якщо хочеш, я можу розписати детальніше розділ про те, як **додавати нові питання**, або створити коротку інструкцію «для вчителя», як користуватися адмінкою під час уроку. Тобі це потрібно?